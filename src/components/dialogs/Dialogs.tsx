// @flow 
import * as React from 'react';
import s from './Dialogs.module.scss';

type Props = {
    
};
export const Dialogs = (props: Props) => {
    return (
        <div>
            Dialogs
        </div>
    );
};