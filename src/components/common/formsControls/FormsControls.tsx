// @flow 
import * as React from 'react';

type Props = {
};
export const Textarea = (props: Props) => {
    return (
        <div>
            <textarea {...props}></textarea>
        </div>
    );
};