import { dialogsPageType, DispatchActionTypes, stateType } from "./state";

export const dialogsReducer = (state: dialogsPageType, action: DispatchActionTypes): dialogsPageType => {
    
    return state;
}